package com.allied.career.day.cutlets.util

interface Command {
    companion object {
        const val MENU = "/menu"
        const val SHOW_CART = "/show_cart"
        const val ORDER = "/order"
    }
}